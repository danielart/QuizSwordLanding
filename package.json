{
  "name": "quizswordlanding",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run copy-assets",
    "preview": "vite preview",
    "build:minify": "npx clean-css-cli -o src/style.min.css src/style.css && npx terser src/main.js -o src/main.min.js",
    "copy-assets": "cp src/main.min.js dist/ && cp src/style.min.css dist/ && cp -r public/* dist/"
  },
  "devDependencies": {
    "vite": "^6.3.1"
  },
  "dependencies": {
    "clean-css-cli": "^5.6.3",
    "terser": "^5.39.0"
  }
}
